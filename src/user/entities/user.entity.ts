export type User = {
    userId: string;
    username: string;
    password: string
}

export type Profile = {
    userId: String;
    fullName: String;
    userName: String;
    email: String;
    password: String;
    createdAt: Date;
    updatedAt: Date;
}